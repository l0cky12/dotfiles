[preferred]
# XDPH owns Hyprland-specific capture; GTK fills the interfaces it does not
# implement. In particular, its Settings backend publishes the color-scheme
# changed by `theme set` to Chromium, Electron, GTK and sandboxed applications.
default=hyprland;gtk;
org.freedesktop.impl.portal.Settings=gtk;
