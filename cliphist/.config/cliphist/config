# cliphist configuration. Flags here replace the CLI defaults.
# The database at ~/.cache/cliphist/db is plaintext. Keep a short, useful
# shared history: this 200-item bound applies to both text and retained images.
max-items 200
