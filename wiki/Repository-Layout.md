# Repository layout

## Packages

Each of these is a Stow package. "Active" means the current Hyprland session
actually starts or binds it; "retained" means the configuration is kept but
nothing in the live session reaches it.

| Package | Deploys to | Status | Contents |
| --- | --- | --- | --- |
| `hypr` | `~/.config/hypr`, `~/.local/bin` | active | Compositor config, keybindings, window rules, autostart, monitor profiles, 44 scripts plus the 9-file capture suite, the theme generator and 23 palettes, and the `theme` / `notificationctl` / `webapp` / `transcode` / `toggle` / `hypr-wallpaper-picker` entry points |
| `quickshell` | `~/.config/quickshell` | active | 95 QML files: bar, panels, notification service, clipboard UI, OSDs, theme picker, Cava visualiser |
| `hyprlock` | `~/.config/hyprlock` | active | Lock-screen layouts and generated colours |
| `kitty` | `~/.config/kitty` | active | Terminal config plus the generated theme include |
| `rofi` | `~/.config/rofi` | active | Comet Glass launcher layout, power-menu and calculator layouts, generated palette |
| `cliphist` | `~/.config/cliphist` | active | Clipboard-history limit (200 text/image entries) |
| `menu` | `~/.config/lmenu`, `~/.local/bin` | active | `lmenu`, a data-driven Rofi menu over a 206-entry JSONC tree, plus `lmenu-reminder` and `lmenu-toggle-ratio` |
| `modes` | `~/.config/desktop-mode`, `~/.local/bin` | active | `desktop-mode`: night light, DND, stay-awake, screensaver-auto, and the expiry daemon |
| `screensaver` | `~/.config/ascii-screensaver`, `~/.config/branding`, `~/.local/bin` | active | `ttfx` ASCII renderer, per-monitor launcher, branding tools, lock handoff |
| `systemd` | `~/.config/systemd/user` | active, opt-in | `hypr-monitor-watch.service` |
| `browser` | `~/.config/*/NativeMessagingHosts`, `~/.local/bin`, `~/.local/share/chromium-tools` | active | Two Chromium MV3 extensions and their native messaging hosts |
| `xdg` | `~/.config/mimeapps.list`, `~/.local/share/applications` | active | MIME defaults and a Kitty/Neovim desktop entry |
| `windows` | `~/.local/bin`, `~/.local/share/applications` | optional | `windows-vm` controller, Dockur Compose template, desktop entry |
| `security` | `~/.config/gnupg-conf`, `~/.local/bin` | optional | `yubikey-auth` and example GnuPG configs |
| `greeter` | `~/.config/greeter` | active, opt-in | Landing spot for the two regreet files `theme set` renders; both gitignored |
| `ai` | `~/.config/ai-agent`, `~/.local/bin` | optional | `ai-agent`, one launcher for Claude Code, Codex, OpenCode, and T3 Code |
| `zsh` | `~/.zshrc`, `~/.p10k.zsh` | optional | Shell config; Oh My Zsh itself is not tracked |
| `fastfetch` | `~/.config/fastfetch` | optional | Normal and Pokémon startup displays |
| `neovim` | `~/.config/nvim/colors` | optional | Generated palette colorscheme and stable `current.lua` alias |
| `btop` | `~/.config/btop/themes` | optional | Generated btop theme and stable `current.theme` alias |
| `obsidian` | `~/.config/obsidian/snippets` | optional | Generated CSS source that must be linked into each Obsidian vault |
| `wallpaper` | `~/Pictures/Wallpapers` (custom target) | optional | Tracked wallpaper assets, including per-theme images |
| `swaync` | `~/.config/swaync` | retained | Rollback notification backend; its autostart line is commented out |
| `wofi` | `~/.config/wofi` | retained | Alternative launcher; not bound, not started |
| `noctalia` | `~/.config/noctalia` | retained | Alternative shell settings and plugin data; startup commented out |

## Not packages

| Directory | What it is |
| --- | --- |
| `docs/` | The original reference notes. Never stow. |
| `wiki/` | This wiki. Never stow. |
| `tests/` | 32 fixture tests. Never stow. See [Testing](Testing-and-Contributing.md). |
| `system/` | Root-owned `/etc` templates: `greetd/config.toml` plus its dry-run installer, and PAM stacks for `sudo`, `doas`, and `hyprlock`. Deployed by `yubikey-auth` and `system/greetd/install.sh`, never symlinked into `$HOME`. Stowing it would create `~/greetd` and `~/pam.d`. |

## Inside the `hypr` package

`hypr` is by far the largest package, and most of the desktop lives in it.

```text
hypr/.config/hypr/
├── hyprland.lua              entry point: monitors, env, input, gestures, requires
├── hyprland.conf             parallel hyprlang graph, kept as a rollback path
├── hypridle.conf             four idle listeners: 180 / 300 / 1200 / 1800 s
├── hyprlock.conf             lock wrapper; sources generated colours + a layout
├── hyprpaper.conf            IPC on, splash off, no preloaded image
├── hyprsunset.conf           identity profile; night-light.sh drives it
├── conf/
│   ├── variables.lua         terminal, browser, file manager, disks, scripts_dir
│   ├── keybindings.lua       every binding (211 lines)
│   ├── window_rules.lua      floating behaviour and workspace assignment
│   ├── autostart.lua         the hyprland.start process list
│   ├── decorations.lua       GENERATED by the theme tool
│   └── greeter/              throwaway Hyprland config that hosts regreet
├── monitor_profiles/         kvm / desktop / laptop / work / presentation pairs
├── monitors.lua              GENERATED machine state
├── workspaces.lua            GENERATED machine state
├── themes/<slug>/colors.toml 23 palettes — the source of truth for appearance
├── theme/
│   ├── generate.py           the renderer
│   ├── themelib.py           palette validation, contrast checks
│   └── templates/            15 per-application templates
├── scripts/                  44 helpers; capture/ and lib/ are subdirectories
├── shaders/night-light.frag  screen shader toggled by hyprland.lua
└── udev/                     optional, manually installed hotplug rule
```

## What is generated, and what is tracked

`.gitignore` draws the line. Everything below is derived from a tracked source
and rebuilt on demand, so a fresh clone will not have it:

| Generated | Source of truth |
| --- | --- |
| `hypr/.config/hypr/conf/decorations.lua` and `.conf` | `themes/<slug>/colors.toml` + `theme/templates/hyprland-decorations.lua` |
| `hypr/.config/hypr/themes/.active/` | the selected palette |
| `kitty/.config/kitty/theme/` | `theme/templates/kitty-theme.conf` |
| `rofi/.config/rofi/color-themes/`, `current-theme.rasi`, `powermenu/theme.rasi` | `theme/templates/rofi-*.rasi` |
| `hyprlock/.config/hyprlock/colors.conf` and `layouts/colors.conf` | `theme/templates/hyprlock-colors.conf` |
| `swaync/.config/swaync/style.css` | `theme/templates/swaync-style.css` |
| `wofi/.config/wofi/style.css` | `theme/templates/wofi-style.css` |
| `noctalia/.config/noctalia/colors.json`, `colorschemes/` | `theme/templates/noctalia-colors.json` |
| `greeter/.config/greeter/greeter.css`, `regreet.toml` | `theme/templates/greeter-theme.css`, `regreet-greeter.toml` |
| `neovim/.config/nvim/colors/<slug>.lua`, `current.lua` | `theme/templates/neovim-theme.lua` |
| `btop/.config/btop/themes/<slug>.theme`, `current.theme` | `theme/templates/btop-theme.tpl` |
| `obsidian/.config/obsidian/snippets/generated-theme.css` | `theme/templates/obsidian-theme.css`; manually linked into each vault |
| `~/.config/zsh/current-theme.zsh` | `theme/templates/zsh-theme.zsh` — written straight into the config dir, not a tracked Stow path |
| `hypr/.config/hypr/monitors.lua`, `workspaces.lua`, `monitors.conf`, `workspaces.conf` | `monitor_profiles/<profile>.*.lua` |
| `zsh/.oh-my-zsh/` | not vendorable; see [Getting started](Getting-Started.md#oh-my-zsh-is-not-in-this-repository) |

Rebuild the first group with `theme set <slug>` and the monitor group with
`auto-monitor-profile.sh --force`.

A tracked `.stow-keep` retains an otherwise empty generated-output leaf.
Packages that could capture application state also keep a tracked `.gitignore`
at the app-config level to ignore runtime files written beside generated output.
That ignore file does not prevent GNU Stow tree folding; deploy `neovim`, `btop`,
and `obsidian` with `stow --no-folding neovim btop obsidian` so app state is not
written through a directory symlink into the checkout.

## Runtime state, outside Git

| Location | Holds |
| --- | --- |
| `$XDG_STATE_HOME/hyprland-desktop/notifications/` | DND flag, restorable active cards, history, bounded image copies |
| `$XDG_STATE_HOME/hyprland-desktop/wallpaper/current` | last successfully applied wallpaper; read by autostart and Hyprlock |
| `$XDG_STATE_HOME/toggles/screensaver-off` | the one deliberately persistent mode override |
| `$XDG_STATE_HOME/lmenu/` | lmenu instance pid and current route |
| `$XDG_STATE_HOME/docker-dev-env/environment.env` | generated dev-database credentials, mode 0600 |
| `$XDG_RUNTIME_DIR/hyprland-desktop/modes/state.json` | desktop-mode state; session-only by design |
| `$XDG_RUNTIME_DIR/windows-vm-$UID/` | VM launch lock and ephemeral credentials |
| `~/.cache/cliphist/db` | clipboard history, unencrypted |
| `~/.cache/bluetooth-control/` | cached device classes, so the Bluetooth poll stays cheap |
| `~/.local/share/webapps/` | web-app metadata, icons, and their `.desktop` launchers |
